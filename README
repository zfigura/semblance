Semblance version 1.0
=====================

Semblance is eventually meant as a set of tools to manipulate assembly
code. Currently it only contains a single program:

dump - produce disassembly and other information from exectable files.
       Currently only supports NE (New Executable) files.


Installing
----------

To build, just run:

    ./autogen.sh
    ./configure
    make

To install to your computer, subsequently run "make install" as root.
